const config = {
  // Tailwind CSS v4 uses the official PostCSS plugin '@tailwindcss/postcss'
  // Plugins should be defined as an object, not an array, so PostCSS can load them correctly.
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
